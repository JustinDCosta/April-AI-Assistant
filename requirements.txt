# Core AI & routing
groq>=1.0.0
ollama>=0.3.0
python-dotenv>=1.0.0

# Web search (import: ddgs)
ddgs>=9.0.0

# Audio: microphone capture & faster-whisper STT
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
faster-whisper>=1.0.0
sounddevice>=0.5.0
soundfile>=0.12.0
numpy>=1.24.0

# TTS & text cleanup (Piper is invoked via subprocess in audio.py)
emoji>=2.0.0

# Optional: classic offline TTS (not used by default Piper pipeline; kept for compatibility)
pyttsx3>=2.90

# Windows automation & media
pycaw>=20240210
comtypes>=1.2.0
pyautogui>=0.9.54
